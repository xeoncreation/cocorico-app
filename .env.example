# Client env (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ey...

# Server env (keep secret)
ADMIN_SECRET=change_me_to_a_long_unique_value
LOG_LEVEL=info
OPENAI_API_KEY=sk-...
# --- Cocorico: ejemplo de variables de entorno ---
# Copia este archivo a .env.local y rellena los valores.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# (Opcional) Service role solo para scripts/servicios, NO exponer en cliente
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI
OPENAI_API_KEY=

# ElevenLabs (TTS)
ELEVENLABS_API_KEY=

# App URL (usada en callbacks, links de Stripe, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Analytics (GDPR-friendly)
# Website ID de Umami desde https://cloud.umami.is (Dashboard → Add website)
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# Stripe
# Clave secreta (test o live según entorno)
# Dashboard: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
# Secret del webhook (Stripe Dashboard → Developers → Webhooks)
STRIPE_WEBHOOK_SECRET=
# Clave pública (cliente)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# ID del precio Premium mensual (ej: price_1PQxyzAbc)
# Crear en Dashboard → Products → Cocorico Premium
STRIPE_PRICE_ID_PREMIUM=
# URLs de redirección tras pago
STRIPE_SUCCESS_URL=https://cocorico.app/dashboard
STRIPE_CANCEL_URL=https://cocorico.app/pricing

# Firebase (Push Notifications)
# Obtener desde Firebase Console: https://console.firebase.google.com/
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
# VAPID Key desde Project Settings → Cloud Messaging → Web Push certificates
NEXT_PUBLIC_FIREBASE_VAPID_KEY=
# Server Key desde Project Settings → Cloud Messaging (solo server-side)
FIREBASE_SERVER_KEY=

# Google Analytics 4
# Obtener desde https://analytics.google.com/ (Measurement ID: G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=

# Google Maps/Places API (si se usa)
# Obtener desde Google Cloud Console: https://console.cloud.google.com/apis/credentials
# Restringir la clave a tu dominio en producción
NEXT_PUBLIC_GOOGLE_API_KEY=

# Admin
ADMIN_EMAIL=

# Password gate (Fortaleza Cocorico)
# Si se define, el sitio requiere contraseña en /access
# RECOMENDACIÓN: mantén activo hasta tener ~20 testers confirmados.
# Cuando tengas ~50 testers, desactiva SITE_PASSWORD y deja solo el sistema de invitaciones.
SITE_PASSWORD=change-me

# Invite password (clave para invitados temporales)
# Permite acceso temporal (24h) usando ?key=INVITE_PASSWORD en la URL
# Ejemplo: https://cocorico.app?key=tester2025
INVITE_PASSWORD=tester2025

# Dev email (acceso a /dev/lab y /dev/audit)
# Solo esta dirección puede acceder a las páginas privadas de desarrollo
DEV_EMAIL=carlos@xeoncreative.com

# Logging externo (opcional, producción)
# LOGGING_WEBHOOK=https://your-discord-or-slack-webhook-url

# Replicate (opcional, visión en la nube)
# Obtén el token en https://replicate.com/account
# NECESARIO para usar el modo "Nube" en SmartCamera (Lab).
# Sin token, el endpoint usa detección local con TensorFlow.js (gratuita).
# En Vercel: agregar como variable de entorno en Settings → Environment Variables
REPLICATE_API_TOKEN=
# Nota: Nunca compartas ni publiques este token. En repos públicos, usa `.env.local`.

# Resend (opcional, emails automáticos de invitación)
# Obtén tu API key en https://resend.com/api-keys
# Usado para enviar emails automáticos con enlaces de invitación beta
# RESEND_API_KEY=

# Node/Next
# NODE_ENV=development
