"use client";
import { ReactNode } from "react";

export function ToastProvider({ children }: { children: ReactNode }) {
  // Placeholder: implement toast logic or use a library if needed
  return <>{children}</>;
}
